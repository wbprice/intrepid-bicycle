'use strict'
/* global describe, it */

const assert = require('assert')

describe('S3signController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['S3signController'])
  })
})
