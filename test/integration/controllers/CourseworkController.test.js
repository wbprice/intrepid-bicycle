'use strict'
/* global describe, it */

const assert = require('assert')

describe('CourseworkController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['CourseworkController'])
  })
})
