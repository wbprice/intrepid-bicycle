'use strict'
/* global describe, it */

const assert = require('assert')

describe('ClassesController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['ClassesController'])
  })
})
