'use strict'
/* global describe, it */

const assert = require('assert')

describe('SnsController', () => {
  it('should exist', () => {
    assert(global.app.api.controllers['SnsController'])
  })
})
