'use strict'
/* global describe, it */

const assert = require('assert')

describe('ClassesService', () => {
  it('should exist', () => {
    assert(global.app.api.services['ClassesService'])
  })
})
